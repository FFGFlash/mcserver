import EmptyState from '../components/emptyState'

export default function Dashboard() {
  return (
    <EmptyState
      title="Oops! Looks like we haven't finalized this page yet"
      description="Check back in later versions for updates!"
    />
  )
}
